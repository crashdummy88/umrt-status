# Cloudflare Pages — umrt-status

1. CF dashboard → Workers & Pages → Create → Pages → Connect to Git
2. Repo: `crashdummy88/umrt-status`
3. Build: framework preset **None** · build command empty · output directory `/` (or `.` if site lives at repo root)
4. After soft scaffold lands on `main`, tip will be `https://umrt-status.pages.dev/` (name may vary)
5. Optional custom domain later: `status.…` when Matt locks DNS
